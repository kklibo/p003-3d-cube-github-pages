fn main() {
    println!("To build the WebAssembly for this project, run:");
    println!("wasm-pack build --target web");
    println!("Then serve the files from the root directory with a web server");
}
